<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="#FFFFFF" /> <!-- Define a cor de fundo, neste caso, branco (pode ser alterada para a cor desejada) -->
    <stroke
        android:width="1dp"
    android:color="#E6E6E6" /> <!-- Define a cor da borda -->
    <padding
        android:left="0dp"
        android:top="0dp"
        android:right="0dp"
        android:bottom="1dp" />
   </shape>
